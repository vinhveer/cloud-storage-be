@baseUrl = http://localhost:8000
// user id 15
@token = 20|y4xWD4RXu0vkC19dloofc7Th167rQyMTj71KHxwjd55bcd1f
// user id 3
@otherToken = 21|rFJVt1xln3wmkPn5AKez8GilVJL1y3yJko4G3kwc032a5ef9
@fileId = 45
@nonExistentFileId = 999999999
@notOwnedFileId = 5

### 1) Happy path — delete my file (should return 200)
DELETE {{baseUrl}}/api/files/{{fileId}}
Accept: application/json
Authorization: Bearer {{token}}

### 3) File not found — 404
DELETE {{baseUrl}}/api/files/{{nonExistentFileId}}
Accept: application/json
Authorization: Bearer {{token}}

### 4) Forbidden — delete file owned by another user (should return 403)
DELETE {{baseUrl}}/api/files/{{notOwnedFileId}}
Accept: application/json
Authorization: Bearer {{otherToken}}

### 5) Unauthenticated — no auth header (should return 401)
DELETE {{baseUrl}}/api/files/{{fileId}}
Accept: application/json

### Notes
# - Replace `@fileId`, `@notOwnedFileId`, and `@otherToken` with values from your environment.
# - Run requests in the order above to reproduce the "already deleted" case (first request deletes the file, second shows the already-deleted error).
# - The responses follow the project's fail()/ok() conventions (JSON with `message` and error code fields).
